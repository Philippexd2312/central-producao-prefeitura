services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: central_producao
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - central_producao_pg:/var/lib/postgresql/data

volumes:
  central_producao_pg:
